import * as React from "react";
import { TextField } from "@mui/material";

export default function WrappedTextEntry({
  value,
  setValue,
}: {
  value: ;
  setValue: ;
}) {
  return (
    <TextField></TextField>
  );
}
